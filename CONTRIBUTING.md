# 🤝 راهنمای مشارکت

از اینکه می‌خواهید در این پروژه مشارکت کنید خوشحالیم! لطفاً مراحل زیر را دنبال کنید.

## مراحل مشارکت

1. **Fork** کردن مخزن
2. ساخت یک شاخه (branch) جدید:
   ```bash
   git checkout -b feature/نام-قابلیت
   ```
3. اعمال تغییرات و تست آن‌ها
4. ثبت تغییرات (commit):
   ```bash
   git commit -m "افزودن قابلیت جدید"
   ```
5. پوش کردن شاخه:
   ```bash
   git push origin feature/نام-قابلیت
   ```
6. ایجاد یک **Pull Request**

## قوانین کد

- از سبک کد **PEP 8** برای پایتون استفاده کنید.
- برای تغییرات بزرگ ابتدا یک Issue باز کنید.
- پیام‌های commit را واضح و توصیفی بنویسید.
- در صورت امکان برای قابلیت‌های جدید تست بنویسید.

## گزارش باگ

هنگام گزارش باگ، لطفاً موارد زیر را ذکر کنید:

- سیستم‌عامل و نسخه پایتون
- مراحل بازتولید مشکل
- رفتار مورد انتظار و رفتار واقعی
- تصاویر یا پیام‌های خطا (در صورت وجود)

از مشارکت شما سپاسگزاریم! ❤️
